[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "dist/functions"
  node_bundler = "esbuild"
  external_node_modules = ["@libsql/client", "@libsql/hrana-client", "drizzle-orm"]
